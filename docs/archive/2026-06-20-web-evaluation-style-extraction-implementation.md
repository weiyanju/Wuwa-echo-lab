# Web 评估样式拆分实施记录

## 范围

- 将模型评估概览、回测诊断、融合权重、模型详情、深色模式和响应式规则迁移到 `src/styles/features/evaluation.css`。
- `style.css` 仅保留共享基础规则和跨功能组合规则。
- 不修改页面行为、API 契约或数据结构。

## 实施结果

- 新增评估样式边界及对应架构测试。
- 更新样式契约测试，使其读取实际的入口与评估样式集合。
- `style.css` 从 5604 行降至 1206 行，评估样式文件为 4524 行。
- 固化入口和评估样式文件的规模上限。

## 验证

- `npm test`：91 项全部通过。
- `npm run build`：生产构建通过。
- 浏览器连接、Vite、Django 与登录页控制台检查正常；由于没有可复用的已登录会话且本轮不创建测试账号，评估页亮色、深色和 860px 窄屏布局仍待补验。
