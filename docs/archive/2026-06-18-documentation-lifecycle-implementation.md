# 文档开发闭环实施记录

## 目标

建立“开发前读取规范、完成后归档、长期事实同步回总规范”的固定文档流程，避免阶段记录、长期规则和实际代码状态互相脱节。

## 实际完成内容

- 在工程质量规范中增加功能开发文档闭环。
- 明确开发前的总规范与专项文档阅读顺序。
- 明确功能完成后的验证、归档和长期规范同步要求。
- 明确不同类型变化应同步到哪一份长期规范。
- 扩展归档目录说明，增加文件命名、固定模板和填写规则。

## 修改模块与文件

- `docs/engineering-quality.md`
- `docs/archive/README.md`
- `docs/archive/2026-06-18-documentation-lifecycle-implementation.md`

## API、数据库与数据边界变化

无。

## 性能与资源影响

无运行时影响。

## 测试与验收结果

- 使用 `rg` 检查新增章节、模板字段和执行约束均已写入目标文档。
- 使用 `git diff --check` 检查 Markdown 变更，没有空白错误。

## 与原计划的偏差

无。实施内容与确认后的文档闭环方案一致。

## 遗留问题

- 既有 `docs/superpowers/plans/` 和 `docs/superpowers/specs/` 暂不迁移，后续按归档说明逐步整理或建立索引。

## 长期规范同步

- 已更新 `docs/engineering-quality.md`，将文档闭环确立为长期工程质量规则。
- 已更新 `docs/archive/README.md`，将归档命名、模板和填写要求确立为归档目录规则。

## 下一阶段入口

下一项功能开始前，先读取顶层总规范和该功能对应专项文档；功能完成后按归档模板记录实际结果并同步受影响的长期规范。
