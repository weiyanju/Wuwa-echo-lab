# 声骸副属性档位与固定值单位修复实施记录

**日期：** 2026-07-30

## 目标

- 将六类共享副属性档位中的 `10.2%` 修正为参考表规定的 `10.1%`。
- 修复历史声骸面板为攻击、生命和防御固定值错误追加 `%` 的问题。

## 实际结果

- 前端与后端共享档位表均已同步为 `10.1`，对应概率继续保持 `0.1456`。
- 修正覆盖普攻伤害、共鸣技能伤害、重击伤害、共鸣解放伤害、攻击百分比和生命百分比六类副属性。
- 历史面板改为复用统一的副属性档位格式化函数；固定值不显示单位，百分比类型继续显示 `%`。
- 新增前端回归测试，覆盖六类共享属性的参考档位以及历史面板的单位格式化入口。
- 更新后端常量测试，防止错误档位再次被测试固化。

## 业务与数据边界

- 未修改各档位概率权重、概率显示精度或运行时归一化逻辑。
- 未修改 API 字段、数据库结构、已有声骸记录或预测算法。

## 验证证据

- 完成失败到通过的 TDD 循环：新增前端测试和更新后的后端测试均先准确复现问题，再由最小实现修复。
- 前端完整测试：350 项通过，0 项失败。
- 后端完整测试：188 项通过，0 项失败（复用现有测试数据库）。
- Vite 生产构建：退出码 0。
